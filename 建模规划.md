# E题建模规划：复杂场景下多模态情感预测

> 状态：实施前方案，已按审计意见修订；修改规划不代表技术验收通过。题面已读取，原稿记载的数据检查和联网查阅结论尚需附可复核记录，本次未重新加载全部数据或复核网页。所有性能数值均须运行后填写，不预设准确率、不宣称算法已经有效。
> 原始输入：`E:\MathModelAgentPi\中文题目\E题`。工作目录：`E:\MathModelWorkspaces\E题`。不修改原始样本与标签。

## 1. 总体目标与实施原则

本题共三个问题，分别解决“特征如何产生和对齐”“局部信息缺失时如何预测”“预测依据如何被复核”。

| 问题 | 输入 | 核心任务 | 必须交付 |
|---|---|---|---|
| 一 | 附件1的100条视频与转写文本 | 自主提取三模态时序特征，建立真实时间映射 | 100条样本的全量特征、时间戳、掩码、汇总表、至少1个对齐案例 |
| 二 | 附件2训练/验证集；附件3专项测试集 | 连续局部缺失下的三分类和强度回归 | 鲁棒模型、消融实验、缺失规律分析、30条专项预测 |
| 三 | 附件2训练/验证集；附件4专项测试集及视频 | 预测与模态/片段归因，回看原始证据 | 可解释模型、忠实度验证、20条预测与解释、案例卡 |

**问题一不必强行复刻附件2的768/74/35维特征。** 题目允许自选开源工具与预训练模型，问题一的重点是合理、完整、可复现。问题二、三可以直接使用附件2，不必用问题一的100条样本训练，也不能擅自将它们加入规定训练集。

推荐先完成简单基线和完整交付链，再增加有证据支持的复杂算法。允许多算法对照，但不是每个候选算法都必须实现。

### 1.1 数据和工具边界

- 模型参数只在附件2的训练集学习；模型结构、超参数和阈值只在验证集选择。
- 标准化、PCA、词表和重采样规则等需学习的统计量只在训练集拟合。
- 保留附件2官方划分；其test不参与调参。模型、解释方法和评估协议冻结后，在官方test统一评估预测、鲁棒性和解释忠实度，作为最终性能结论的主要依据；与开发阶段验证结果、无标签专项结果分开。若源视频跨划分重叠，应说明这不是源视频独立的泛化检验。
- 附件3、4只做结构兼容性检查及最终推理，不依其内容挑选模型、调整阈值或选择最有利的解释。
- 不能通过附件3文本检索附件2找回标签、完整特征或被遮蔽内容；即使存在重复也不得借此补答案。
- 允许通用预训练特征提取器，不额外引入情感数据进行训练或微调。避免下载已在额外情感数据上训练的任务专用预测权重作为主模型。
- 工具开源不代表所有模型权重都可任意使用；分别记录软件、权重、训练来源与许可证。
- 附件总大小不超过50 MB；提前做体积预算，不能最后删除题目要求的材料来凑大小。

## 2. 数据结构记录与待解决风险

### 2.1 原稿记录的数据结构（待复核证据）

以下保留原稿的数据读取记录，不视为本次重新核验的结果。S0须输出文件哈希、检查命令、字段/形状/类型、样本计数和异常清单，并为各项结论附证据路径；复核不符时更新本节。

附件1：100个视频，`label-100.xlsx`的label表为100行，字段为video_id、clip_id、text、label、annotation。另有“修复说明”表，不作为样本。

附件2：两个Pickle均按`data[split][field][sample_index]`读取，训练3395条、验证728条、测试727条。

| 字段 | 对齐版 | 非对齐版 |
|---|---|---|
| text | (N,50,768)，float32 | 同左 |
| text_bert | (N,3,50)，int64 | 同左 |
| audio | (N,50,74)，float64 | (N,500,74)，float64 |
| vision | (N,50,35)，float64 | (N,500,35)，float64 |
| raw_text / id | 原文数组 / 编号列表 | 同左 |
| classification_labels | (N,)，编码0/1/2 | 同左 |
| regression_labels | (N,)，范围[-3,3] | 同左 |
| audio_lengths / vision_lengths | 无 | 有 |

对齐版约948 MiB，非对齐版约2763 MiB。训练前按需转换float32；不要同时复制加载所有大数组。

**附件3接口与题面概括并不完全一致：**

- 对齐版30个文件，每个为`{'test': {'text_bert': (1,3,50), 'audio': (1,50,74), 'vision': (1,50,35)}}`，全部float32。没有预计算text、原文、编号和标签。
- 非对齐版30个文件，每个test下只有raw_text、audio、vision；音视频为(1,500,D)。没有text、text_bert和有效长度。
- 对齐版text_bert不能作为连续语义特征直接输入；词元编号经合法性检查后转换整数，通过匹配的文本编码器处理。

附件4：每版20个文件，直接包含样本字段，无test外层、无批次维度。对齐版有raw_text、id、text、text_bert、audio、vision；非对齐版另有audio_lengths和vision_lengths。各配20个视频。未发现显式逐词时间戳。

原稿记载附件3、4数值字段未发现NaN/Inf，但尚未附检查输出。S0须复核该结论，并完成附件2全量数值质量审计。

### 2.2 在训练前必须关闭的风险

| 风险 | 错误做法 | 正确处理与通过条件 |
|---|---|---|
| 专项文本字段缺失 | 用附件2的text训练，再给附件3硬补零 | 先打通同一编码路径；确认词元词表、特殊符号、大小写规则与掩码 |
| 填充和缺失混淆 | 所有零行都计作人工缺失 | 分离padding、原生无效、人工遮蔽；无法区分时报告不可辨识性 |
| BERT上下文泄露 | 完整文本编码后仅置零局部向量，声称模拟转写缺失 | 实际词元缺失应在编码前遮蔽；另将编码后遮蔽定义为特征丢失实验 |
| 时间映射缺失 | 用序号/50乘视频时长当精确时间 | 校验特征位置语义与词元/单词映射；无法确认时不能声称精确定位 |
| 跨划分源视频重复 | 同一源视频的片段当完全独立样本 | 保留官方划分，报告video_id重叠；置信区间按源视频分组抽样 |
| 分类口径错误 | 把0同时计入负向或正向 | 0为中性；全量校验三分类编码与回归符号 |
| 模型文件过大 | 把数百MB预训练权重直接打包 | 早期核对依赖权重是否可外部获取；不能自行豁免50MB规则 |

### 2.3 推荐的版本选择

先验证**对齐版**：序列短、训练成本低，附件3至少提供text_bert，较容易统一文本输入。非对齐版作为后备研究路线，不因模型名字更先进就优先使用。

必须先在附件2训练/验证数据验证tokenizer身份，不能仅凭768维或101/102编号断言原模型必为某个BERT。若无法核实，应向赛题方确认；重新编码是新定义的表示，不能宣称复现官方预计算text。

问题二若选冻结BERT重新编码，应对train、valid、冻结后的test及专项输入全程采用同一权重、层、截断和掩码规则。附件3遮蔽的词元不得从其他文件还原。问题三也尽量沿用这条路径，减少两套语义空间造成的解释误差。

### 2.4 S0/S1前置验收门槛

以下门槛均为待证据。S0/S1允许小样本试验；相关门槛未通过，不启动受影响路线的全量提取或正式训练。能独立开展的工作可继续，但不能据此宣称三问均可交付。

| 门槛 | 通过证据 | 未通过时的动作 |
|---|---|---|
| G1 文本与掩码接口 | tokenizer核验、训练/验证/专项结构适配、掩码传播及占位值不变性检查通过 | 暂停问题二、三正式训练；查官方说明或核验另一特征版本，不猜词表或恢复专项缺失内容 |
| G2 原素材定位 | 官方处理规则/源码版本或答复，及可核查的词元—词—特征位置关系；涵盖特殊符号、截断、填充和音视频时间依据 | 暂停将该路线定为问题三最终方案；请求补充映射，或验证其他合规路线。无可核验方案则标记第三问定位未解决，不以近似映射冒充完成 |
| G3 提交体积与依赖 | 外部权重提交规则依据、依赖清单、小样本特征体积估算及试打包记录；完整提交预算满足50MB并留余量 | 暂停受影响路线的全量提取/正式训练；改用能满足接口和体积约束的工具或模型，不擅自省略必需权重 |

G2规则在官方说明及附件2训练/验证数据上核验；附件1可验证强制对齐工具，但不能证明附件2的历史处理规则。附件4在此阶段只检查结构兼容性，冻结后才生成并复核逐样本证据。早期通过G2表示定位路径有依据，不替代最终20条定位验收。

## 3. 联网调研结论与方法选择

以下保留原稿记载的资料入口与能力摘要。本次未复核访问记录；实施前需补充访问日期、版本/提交号、关键结论及对应来源，工具选型还须以本地小样本试验为准。

| 资料 | 查阅到的能力 | 本题用途 |
|---|---|---|
| Montreal Forced Aligner官方文档 | 用已知转写、发音词典和预训练声学模型强制对齐 | 问题一逐词时间边界首选 |
| WhisperX官方仓库及alignment.py | align接口支持将已知转写与音频对齐 | MFA部署困难时的备选，不必重新识别并替换原文 |
| BERT base uncased模型卡 | 通用英文自监督预训练表示 | 问题一文本特征；二、三需先确认词元兼容性 |
| openSMILE Python文档 | eGeMAPSv02有25维LLD和88维Functionals | 首选25维帧级声学特征，保留时间序列 |
| OpenFace官方仓库 | 面部关键点、头姿、动作单元和视线估计 | 语义明确的视觉特征 |
| MediaPipe Face Landmarker | 人脸关键点、表情系数和变换矩阵 | OpenFace部署失败时的轻量替代 |
| CMU Multimodal SDK | 特征与时间区间联合存储、按参考模态聚合 | 借鉴对齐规则；不下载额外数据补训练集 |
| MulT，ACL 2019 | 非对齐多模态序列的跨模态Transformer | 非对齐后备模型；不是当前主线必需项 |
| Integrated Gradients论文与Captum文档 | 对连续输入的梯度积分归因、完整性检查 | 问题三片段级解释 |
| Is Attention Interpretable? | 注意力大小不是可靠的唯一解释 | 注意力只做辅助展示，必须做扰动验证 |

注意：openSMILE研究教育使用与商业使用许可不同；OpenFace及其依赖/训练模型也需核对许可。不能把25维eGeMAPS声称为附件2的74维声学特征，也不能猜测35维视觉特征对应哪些AU。

原稿记载未获得题面所列新论文的可核实搜索结果，本次未重新检索。不把尚未核验的2025/2026论文作为已复现方法，也不补造引用。

## 4. 问题一：三模态特征提取与时序对齐

### 4.1 目标、输入与假设

目标不是先追求情感分类准确率，而是生成覆盖全部100条样本、具有真实时间依据、可重新运行的特征数据。

输入是视频、原始英文转写及标签。标签只保留用于追溯，不用于选择对齐边界。以片段内部时间为统一时间轴，单位秒；保留原视频帧时间戳，不假设所有视频恒定帧率。转写与语音大体对应是待验证假设，而非既定事实。

### 4.2 候选算法与推荐组合

| 模块 | 推荐 | 备选 | 选型依据 |
|---|---|---|---|
| 媒体解析 | FFmpeg/ffprobe | 已安装的视频库 | 读取音视频时长、帧时间戳和采样信息 |
| 转写对齐 | MFA英文预训练声学模型 | WhisperX对齐模块 | 已有真实文本，不必先做ASR再替换文本 |
| 文本 | 冻结BERT词元表示，子词聚合为词表示 | 小型通用英文Transformer | 语义能力、部署成本、许可证 |
| 语音 | openSMILE eGeMAPSv02 LLD，25维 | librosa的MFCC、能量、过零率等 | 前者有清楚的声学定义；后者作为低依赖基线 |
| 视觉 | OpenFace的AU/头姿/视线子集 | MediaPipe表情系数和头姿 | 优先可解释，不直接使用外部情感分类器 |
| 跨模态对齐 | 词时间区间内的重叠加权聚合 | 固定时间窗聚合 | 主线词级对齐便于解释，固定窗只作对照 |

先挑3—5条覆盖短片、长片、低声量、人脸缺失情况的附件1样本做可行性试验。通过后跑全部100条。不要同时安装所有候选工具。

### 4.3 实现流程

1. 建立样本清单，核验video_id、clip_id与Excel一一对应；记录文件哈希、时长和媒体信息。
2. 解码音轨。按声学对齐器要求重采样，例如16 kHz单声道；保留变换参数和原音轨时间偏移。
3. 保留原始转写，另生成规范化副本及字符映射；将缩写、标点、数字的处理规则写入日志。
4. 将已知转写与音频强制对齐，输出每个词的起止时间、对齐状态和可用质量信息。
5. 提取BERT子词表示，按tokenizer的offset映射聚合到词；特殊符号不伪装成真实词。长文本分块编码后合并，不能为方便只保留前50词而声称全量覆盖。
6. 提取帧级声学描述子，保留每帧时间区间。不能仅提取整段88维统计量代替时序特征。
7. 提取视觉特征，保留帧时间、检测置信度、跟踪身份及失败标记。多个人脸时用稳定跟踪和预先声明的规则选主体，不能逐帧任意切换。
8. 将音视频帧聚合到词级时间区间，输出各模态观测掩码；无人脸不等于情感为零。
9. 保存变长词序列。后续批处理若需padding，再显式生成padding mask；不要破坏原始长度信息。
10. 生成全量汇总、对齐示例及异常处理清单。

### 4.4 数学定义

第i个视频时长为D_i，第k个词区间为I_ik=[s_ik,e_ik]，满足0≤s_ik<e_ik≤D_i。声学或视觉第j帧区间为J_ij，描述子为u_ij，检测有效性为q_ij∈{0,1}。

定义重叠权重：

`w_ikj = length(I_ik ∩ J_ij) × q_ij`

词级特征为：

`x_ik = Σ_j w_ikj u_ij / Σ_j w_ikj`

当分母为0时，输出零占位、观测掩码0及失败原因，不将其当有效零值。文本词表示为映射到该词的子词向量均值；保存原文字符区间和子词索引。音频与视频分别聚合，不假设两者原始采样率一致。

对于近似瞬时采样的视频帧，以相邻帧时间中点构造支持区间并裁剪到[0,D_i]。该规则必须记录；若采用最近帧，则单独声明其时间误差范围。

### 4.5 异常与替代方案

- 对齐失败：换备选对齐器、人工核对原文与语音；保留失败项，不能删除样本。
- 词边界插值：只能用于可说明的局部失败，记录插值标记；均匀分摊整段时间不能充当强制对齐成功。
- 无语音、无人脸：保留样本和可用模态，显式标记观测失败；讨论对可用性的影响。
- 背景音乐或多说话人：记录问题，人工检查主体对应。声源分离不是默认步骤，只有确有需要再添加。
- 大量人脸检测失败：先检查解码、分辨率和追踪，再评估MediaPipe备选；不得在成功样本上报全数据效果。

### 4.6 实验、交付与验收

建议比较“词级聚合”和“固定窗聚合”的证据可读性；对齐器不必全量跑两个，先在有人工边界的子集比较。人工核验至少覆盖10条不同长度和质量的视频，其中至少1条在论文正文完整展示。人工标注文件保留复核依据，不参与情感模型调参。

| 交付文件（建议） | 内容 |
|---|---|
| problem1/features/*.npz | 100条样本的变长三模态特征、词区间、掩码 |
| problem1/manifest.csv | 每条样本的ID、时长、各维度、长度、成功/失败状态 |
| problem1/alignment/*.json | 原文、词/子词映射、时间戳、检测与对齐质量 |
| problem1/quality.csv | 时间合法性、边界误差、有效覆盖率及异常 |
| problem1/examples/ | 文本—波形—视频帧—特征的同轴示例 |

**硬验收：**

- 100/100条样本均有记录、输出和状态；编号唯一且与输入逐条对应，标签未改动。
- 全部时间区间合法，数组长度与时间映射一致；无未解释NaN/Inf。
- 区分“样本覆盖100%”与“所有模态提取成功率”，后者必须按实际值报告，不能用占位冒充成功。
- 每个失败或插值位置有标记；每个非缺失特征能追溯到原素材区间。
- 至少1个真实样本的三模态对应展示可人工复核；论文包含全部100条汇总信息。
- 工具、权重、参数和运行命令锁定；随机抽取样本重跑可复现，浮点差异有明确容差。

**内部质量目标，非题面门槛：** 人工检查子集的词边界误差中位数争取≤0.15秒，90分位数争取≤0.30秒；起止边界分开统计。未达到就复核和修正，不将目标值当结果。无人工真值时只报时间合法性和人工核验，不伪造误差。

## 5. 问题二：局部模态缺失下的鲁棒预测

### 5.1 目标与输入统一

同时输出负向/中性/正向三分类和[-3,3]情感强度。核心问题是连续局部缺失，不是仅做整个模态丢弃。

优先采用对齐版。训练前完成文本兼容性试验：核对附件2词元与原文、tokenizer及特殊符号规则；验证附件3浮点词元为合法整数值，识别已有注意力掩码和遮蔽形式。若词表身份不明，先请求澄清，不能猜测后默认为正确。

推荐冻结兼容的通用文本编码器，对附件2和3的text_bert均重新编码。明确该表示不必等同于官方text。标准化后重新施加观测掩码，避免原来零占位经标准化变成非零有效输入。

统一内部接口为：

`{sample_id, text_features, audio, vision, padding_mask, observed_mask, mask_status, length_info, labels(optional)}`

附件3以文件名作sample_id。逐模态定义padding_mask=1为填充，observed_mask=1为可用观测；有效位置标记p=1−padding_mask，最终有效掩码b=p·observed_mask。length_info保存原始长度/长度来源；无法核实时保留未知值。mask_status记录原生无效、人工遮蔽、填充或推定不可用及其依据，不把推断写成真实缺失原因。

**掩码传播规则：**

1. 在改动词元或标准化前保存原始注意力掩码、有效长度信息和人工缺失清单。注意力掩码为0本身不能区分padding与内部缺失；尾部零区间尤其不能擅自恢复真实长度。
2. 编码前将不可用词元替换为固定合法占位ID，并按已核实的编码器规则设置注意力掩码；不删除位置导致后续索引左移。特殊符号可供编码器使用，但不作为真实证据或观测词参与下游池化。
3. BERT输出中的缺失位置仍可能非零。编码后再次应用原始有效掩码；下游编码器输入、池化、门控比例和归因候选区间均沿用该掩码，不能依据隐藏向量是否为零重新推断可用性。音视频也在标准化后重新施加掩码。
4. 不可辨识区间按冻结规则标记为推定不可用，统一排除其观测值；长度未知时不报告真实缺失率。可用比例只能按已知或明确声明的推定支持区间计算，并记录分母来源。相关假设在训练/验证数据上检查，不能通过专项结果选取。
5. 全无有效文本时跳过普通文本编码或使用经过检查的安全路径，设置文本不可用标记；不得把特殊符号的输出当作可用文本。

**G1最小检查：** 在eval模式、固定种子和相同掩码下，替换被屏蔽位置的合法占位输入，最终预测应在预先记录的浮点容差内不变；分别覆盖文本、语音、视觉、整模态不可用及全部不可用。另检查内部缺失不改变保留位置索引，padding和特殊符号不进入证据候选。

### 5.2 多算法实验梯度

| 编号 | 算法 | 用途 | 优先级 |
|---|---|---|---|
| B0 | 训练集多数类；训练集强度中位数 | 最低参照，防止复杂模型不如常数 | 必做 |
| B1 | 单模态掩码池化 + Logistic/Ridge | 分别检查文本、语音、视觉价值 | 必做 |
| B2 | 三模态池化拼接 + 小型MLP双任务头 | 简单融合基线 | 必做 |
| R1 | 掩码感知时序编码 + 动态门控 + 连续片段缺失增强 | 主模型 | 必做 |
| R2 | R1加完整/缺失一致性训练 | 检查稳定性增益 | 主线扩展 |
| R3 | 跨模态重建辅助损失 | 检查缺失恢复是否有帮助 | 时间允许 |
| R4 | 小型跨模态Transformer/MulT思路 | 表达能力对照；非对齐版本的后备 | 非必需 |

B1可用同一冻结文本编码器得到的词元表示，避免比较中混入不同预训练模型优势。R1先用一层GRU或小型时序卷积，不默认堆叠大型Transformer。

最低公平对照如下，不用增加更多模型名称替代控制变量：

| 对照配置 | 保持一致的条件 | 可回答的问题 |
|---|---|---|
| B2，无缺失增强 | 与下一组相同的池化、MLP及文本表示 | 干净训练的简单基线 |
| B2，同等缺失增强 | 与R1相同的训练缺失分布及比例 | 简单模型能否仅靠增强获得鲁棒性 |
| R1骨干＋固定融合＋同等增强 | 与动态组相同时序骨干、维度、双头和掩码 | 动态门控之外的时序模型收益 |
| R1骨干＋动态融合＋同等增强 | 只改变融合规则 | 动态门控的增量收益 |

固定融合对可用模态使用预先声明的固定权重并重新归一化；不可用模态排除，全部不可用使用同一回退规则。各组共用冻结文本表示、评估遮蔽清单和种子集合，记录参数量、训练耗时及相近的调参预算。R1相对原始B2的差异只解释为整套方案收益，不能全部归因于门控。

### 5.3 主模型数学结构

令m∈{T,A,V}，原始输入为x_mt，位置有效标记p_mt=1−padding_mask_mt，观测标记o_mt，有效掩码b_mt=p_mt·o_mt。标记的已知/推定状态遵循§5.1。

1. 各模态线性投影至共享维度d，编码器输入包括特征、掩码和位置。初始候选d取32/64/128，由验证集选择。
2. 时序编码得到h_mt；使用有效位置池化获得z_m。全无有效观测时输出零占位并设置不可用标记，不新增永远被门控排除的可学习向量。
3. 以z_m、可用比例r_m和有来源的质量指标产生门控分数g_m；不得虚构附件未提供的质量分数。已知支持区间时r_m=Σ_t b_mt/Σ_t p_mt，分母为0时置0；未知支持区间按§5.1记录代理口径。对完全不可用模态禁止分配融合权重；全部不可用时分类返回训练集类别频率π，回归返回训练集强度中位数μ，避免softmax全负无穷导致NaN。
4. `α_m = masked_softmax(g_m)`，`z = Σ_m α_m z_m`。
5. 分类头`p=softmax(W_c z+b_c)`；回归头`ŷ=3 tanh(w_rᵀz+b_r)`。

训练损失：

`L = CE(y_class,p) + λ_r Huber(y,ŷ) + λ_cons L_cons + λ_rec L_rec`

先令λ_cons、λ_rec为0跑通R1，再逐项加入。类别权重仅根据训练集分布确定，是否启用由验证集决定。

一致性项可使用冻结教师或停止梯度的完整输入预测作目标：

`L_cons = (ŷ_missing − stopgrad(ŷ_full))² + KL(stopgrad(p_full) || p_missing)`

强缺失下信息确实不足，不能强行要求所有输出与完整输入相同；对缺失强度分层调权，并检查是否损伤中性类或少数类。

若加入重建项，只对训练时人工遮掉、且原本观测有效的位置计算。不能用专项样本的任何完整对应数据监督重建。

双头可能给出符号不一致的结果。记录不一致率；分类以分类头为主，回归单独报告。可在验证集比较“回归加中性阈值”的替代方案，但阈值冻结后不得用专项结果修正。

### 5.4 缺失增强与规律实验

在训练集原有有效区间内采样连续块，不能把尾部padding计入被遮蔽数量。完整样本与缺失样本混合训练，避免模型只适应破损输入。

分两种机制实验：

- **特征丢失：** 编码后的局部向量不可用，模拟中间特征传输丢失。
- **输入丢失：** 文本在编码前遮蔽词元，重新编码，模拟转写局部不可用。掩码标记不可意外恢复原词。

两种机制不能混报为同一实验。附件3的生成机制尚不完全明确，应在论文说明适配假设；有官方说明时以其为准。

主实验设计：

| 因素 | 候选水平 |
|---|---|
| 缺失模态 | T、A、V、TA、TV、AV、TAV |
| 缺失比例 | 0、10%、30%、50%；70%作为压力测试 |
| 缺失位置 | 前部、中部、后部、均匀随机起点 |
| 连续性 | 一个连续块；多个短块作为对照 |
| 多模态关系 | 已核实共同对齐网格上的同步缺失；独立位置缺失 |

同步缺失须先通过G2中的跨模态网格核验。仅三模态长度相等或编号相同，不足以声称同一时间段缺失；未经核实的实验只能称“同索引遮蔽”。词与子词网格不同则按已核实映射定义共同区间，不能直接复用索引。

不默认做全部笛卡尔积：先在固定中等缺失率比较模态，再对主要场景画缺失率曲线，再研究位置和连续性。每项只改变一个主要因素。所有算法共用相同的验证遮蔽清单，保证可配对比较。

附件2没有真实时间戳时，缺失长度以有效序列位置比例度量，称为“相对序列长度”，不能直接换算成真实秒数。若题目要求物理缺失时长，应请求补充映射，或明确采用位置比例代理的局限。

必要消融：去掉缺失增强、去掉供模型使用的显式观测标记、动态门控改固定权重；只有采用一致性或重建扩展时才做对应消融。去掉观测标记不等于删除padding保护、输入遮蔽或数据泄露防护。

### 5.5 训练、评估与停止规则

- 先用少量训练样本跑一个epoch，测显存、耗时、数值稳定性，再确定完整运行预算。
- AdamW可作为默认优化器；学习率初筛1e-3、3e-4，批大小按实际显存定。最多100个epoch、验证指标连续10轮无改善早停，均为初始计划而非已运行设置。
- 用验证集MAE作为预先声明的主选择指标，同时报告Macro-F1；鲁棒模型用预定义缺失场景的平均MAE选择，检查干净输入性能下降。不要事后随意混合指标选最有利结果。
- 最终候选至少3个训练种子；验证缺失清单固定，可另设遮蔽种子检查对缺失位置的敏感性，但这不构成独立样本测试。报告均值、标准差和逐场景结果。
- 冻结全部选型后，在附件2官方test上按预先登记的场景、权重和新遮蔽种子评估；所有对照共用测试遮蔽清单。该阶段不得按test选模型、挑训练种子、改阈值或解释配置。提交模型采用预先冻结的种子或聚合规则。
- 记录冻结配置和权重哈希。若测试后发现实现错误，保留原记录并披露修复与重跑；不能把接触过结果后的调参仍称为未触碰的独立评估。
- 分类报告Accuracy、Macro-F1、Weighted-F1、各类召回率、混淆矩阵；回归报告MAE、Pearson。常数预测导致Pearson未定义时如实标NA。
- 最终差异区间在冻结后的test预测上按源video_id分组进行配对bootstrap；若分组不可得，则报告样本级区间及相关性限制。训练种子离散程度与样本抽样区间分开报告，不能把同一样本的多次遮蔽当独立样本。开发阶段验证集区间仅作描述，不消除选型偏差；分组bootstrap也不修复跨划分重复带来的泛化限制。
- 对齐版时序编码大致随N·L·d²增长；跨模态注意力含L²项。L=500路线须先基准测试，不能以算法先进为由忽略成本。

### 5.6 交付与验收

交付：模型权重、冻结编码器配置、预处理参数、训练配置、验证/test预测及分开统计的指标、缺失清单、冻结记录及`attachment3_predictions.csv`。

CSV建议列：`sample_id, sentiment_label, sentiment_score, prob_negative, prob_neutral, prob_positive, status`。这是自定义建议格式；若官方后续提供模板，以官方模板为准。

**硬验收：** 30/30条预测、编号无重复、三分类合法、强度在[-3,3]、概率和为1、无NaN/Inf；相同输入与权重可复现；无测试集调参；缺失类型、位置、长度均有实验；失败样本不能悄悄省略。

**模型采用目标：** 在验证集比较R1/R2与同等缺失增强的B2，再结合固定融合对照判断复杂度是否值得，报告干净输入的性能代价。复杂模型在验证阶段不优于简单模型时保留简单模型。最终性能结论以冻结后的test为主；若改进区间跨0，不能声称显著提升，即使区间不跨0也需交代评估范围和多场景比较限制。test结果不支持改进时如实报告，不据此重新选型。

## 6. 问题三：可解释预测与关键证据定位

### 6.1 目标与路线

对每条附件4样本给出情感极性、强度、三模态作用程度、主要参考模态以及可回看文本/音频/视频的证据。解释的是模型的决策依据，不是证明人的真实心理原因。

训练仍只使用附件2训练集，结构与解释配置只在验证集选择。问题二、三可共用同一预测模型和权重，分别提供鲁棒性实验与解释流程，并记录共享权重哈希、调用配置和交付对应关系。只有验证证据支持时才单独训练问题三模型，不为形式上区分两问强制复制模型。

最低主线为模态Shapley＋局部片段遮挡，IG作为可选对照。局部遮挡用于排序时，不能再用同一窗口、同一替代方式的分数证明自身忠实；应另设预先固定的联合区间干预或另一替代机制，并与等长随机、低重要性区间比较，避免循环验证。

候选路线：

| 方案 | 优点 | 限制与选择 |
|---|---|---|
| 模态门控/时间注意力可视化 | 易实现，直观 | 不能单独作为解释可信的证据 |
| 三模态精确Shapley | 只有3个模态，8种联盟即可计算 | 依赖缺失替代基线，不是因果结论；主推模态级解释 |
| 片段遮挡消融 | 直接检查删除证据是否改变输出 | 会产生分布外输入；主推独立验证 |
| Integrated Gradients | 适合连续特征，提供正负贡献 | 可选对照；须固定离散掩码并核验积分路径 |
| 可加性小模型 | 分解清晰、容易复核 | 可能损失交互表达能力；性能不足时不强求 |

### 6.2 明确解释目标

每个解释文件必须标明目标：

- 回归解释：f(x)=预测情感强度。
- 分类解释：f(x)=完整输入下所预测类别的固定logit或概率。

删除某模态后不能切换成新的预测类别再比较，否则归因目标发生变化。回归的负贡献表示拉低强度；分类的负贡献表示降低当前目标类别支持，两者含义不同。

### 6.3 模态级精确Shapley

设M={T,A,V}，联盟价值v(S)为仅保留S中模态时的固定目标输出。对每个模态m：

`φ_m = Σ_{S⊆M\{m}} [|S|! (3−|S|−1)! / 3!] × [v(S∪{m})−v(S)]`

所有8个联盟都计算，得到相对于基线的模态贡献。验证：

`Σ_m φ_m ≈ v(M) − v(∅)`。

采用缺失干预时，将不在联盟内的模态观测掩码置0，保留原始padding与位置，使用与预测一致的屏蔽规则。空联盟按§5.3回退：回归v(∅)=μ；分类默认解释固定目标类别c的概率，v(∅)=π_c。若选logit，须先定义包括空联盟在内的一致分数函数，不能把logit与概率混入同一联盟表。

Shapley和式残差只检验分解计算，不证明联盟输入合理或解释忠实。局部缺失训练未必覆盖整模态缺失；验证阶段须检查7种非空联盟的数值稳定性、预测退化及空联盟回退，同时比较替代基线对贡献符号、主导模态的影响。若不稳定，可在训练集上加入少量整模态增强并在验证集选择，或采用仅由训练集构造的背景替代；不得依据附件4挑选策略。

背景替代定义的是另一联盟价值函数，须注明背景样本、掩码/长度处理、固定种子和聚合方式。此时v(∅)为背景输出，不再直接等于π_c或μ；8个联盟计算精确不代表有限背景估计没有误差。最终冻结选定的价值函数，并在test报告联盟检查与忠实度。模态整段删除是解释探针，不改变问题二以局部缺失为核心的任务。

作用程度可写为`a_m=|φ_m|/Σ|φ|`，同时保留φ_m正负号。若分母接近0，标记“无明确主导模态”，不要任意指定文本。主要参考模态默认为绝对贡献最大者，注明它可能是反对当前输出的主要证据。

### 6.4 局部归因

**默认方法：局部片段遮挡。** 在有效位置上按冻结的窗口长度和步长枚举连续区间W，以固定目标计算有符号贡献`s_W=f(x)−f(D_W(x))`。D_W使用预先声明的缺失或背景替代规则；缺失干预保留位置、更新观测掩码及可用比例，不移动后续索引。正值表示该区间支持当前目标，负值表示反对。重叠窗口选择Top-k时按冻结规则去重，贡献不直接相加为整体输出差。

文本输入丢失干预须遮蔽原词元并重新编码；仅置零隐藏表示时明确标为特征丢失，不能声称消除了其他位置已经编码的上下文信息。用于独立忠实度检验的联合区间或替代机制遵循§6.1、§6.6，不重复用排序分数本身作为证据。

**可选方法：条件IG。** 将模型写为f(x;q)，x为连续特征，q为固定的padding/观测掩码、可用比例、位置及其他离散状态。积分全程固定q，不按插值后的数值是否为零重新判定缺失；动态门控仍可随连续特征正常变化。

对连续特征x、基线x'，条件IG定义为：

`IG_j=(x_j−x'_j) ∫₀¹ ∂f(x'+α(x−x');q)/∂x_j dα`。

x'只在有效、可解释的位置取零值或训练集统计背景，其他位置保持相同占位。该基线表示“相同观测状态下的替代特征”，不等同于删除模态。若前向路径仍有随x切换的离散分支，应先处理该不连续性；不能仅增加积分点就宣称解决。

对每个时间位置跨特征维求和，得到有符号贡献；跨维绝对值汇总仅用于重要性排序，不能混同有符号贡献。文本只能在连续嵌入或隐藏表示上积分，不能对整数token ID求梯度。

先取32或64个积分点，增加至128检查收敛与完整性残差：

`δ=|Σ_j IG_j−(f(x;q)−f(x';q))|`。

同时记录绝对残差及相对残差；分母接近0时不以相对误差单独判断，容差在验证阶段冻结。比较零基线与训练集统计基线，背景数据只来自训练集。大残差或基线敏感时，以片段遮挡结果为主，标明IG不稳定。

IG解释固定q条件下的连续特征变化，模态Shapley解释所声明联盟干预的变化；两者通常不共享基线。不得要求IG总和等于相对空联盟的输出差，也不得把IG与Shapley贡献直接相加。

若文本使用带上下文的预计算表示，词位置归因仅表示该上下文表示的重要性，不能直接宣称是原始词的独立因果作用。进一步删除原词并重新编码，是验证原词作用的另一项干预。

### 6.5 时间证据映射：先验证，不能硬凑

附件4提供视频，但特征中没有显式逐词时间戳。问题一工具可对附件4原文与视频强制对齐，建立原词到秒的映射；这属于最终解释处理，不用于调参。

但“重新做出词时间戳”还不足以证明附件4音视频特征的第k行对应第k个词：必须核查官方特征采用词、子词还是重采样位置，是否含特殊符号、截断及填充规则。

实施顺序：

1. S0/S1利用附件2训练/验证样本及官方处理说明、对应版本源码或答复核验位置语义，保存token—word—音视频网格关系。仅凭通用SDK的默认行为不能证明本题文件采用相同处理。
2. 在G2记录特殊符号偏移、词/子词聚合、截断、padding和时间依据。缺失项立即请求澄清；未通过则暂停问题三最终路线选定，不等到S5再发现定位不可做。
3. S5在验证数据上冻结解释和映射配置；S6对附件4执行固定的强制对齐与映射，输出词段、秒区间、视频帧号和映射质量。时间误差仅在有人工核验边界等参照时计算，否则记录未知；不根据专项解释结果重新选择规则。
4. 文本证据可在词元映射核实后定位；音视频无法核实时仅报告“特征位置区间”。只有存在可说明的时间依据时才给候选回看片段，标为近似且不计为定位验收通过。
5. 若主导模态的原素材映射仍不可核验，记录该样本的定位未完成及原因，不用漂亮热图替代验收。

非对齐版只有有效长度也不能保证帧间隔严格均匀；线性缩放到整段时长只能是明确声明的近似，不能自动视为通过。

### 6.6 忠实度、稳定性与案例

在验证集先确定解释算法、窗口长度、Top-k比例、基线和检验规则，之后冻结。在官方test执行预先登记的忠实度、稳定性和联盟检查，作为最终解释结论的主要依据；不能测试后重选解释设置。附件4只按固定规则生成解释。

- 删除Top-k重要区间，与同长度随机区间、低重要性区间比较目标输出变化；比较10%、20%、30%等少量预设比例。
- 对有符号贡献分别验证支持证据和反对证据；不要求删除反对证据也降低目标输出。
- 对分类固定原始目标类别，对回归记录预测强度变化及方向。不能仅用概率降低描述所有任务。
- 比较相邻窗口、轻微输入扰动与不同随机种子的主导模态/Top-k区间一致性。
- 比较至少两种替代基线，避免所有解释只反映“零向量很异常”。
- 完整展示正确、错误、中性、模态冲突等验证案例，分析解释失效；附件4无真值，不称“预测正确”。

删除试验也可能产生分布外输入，因此忠实度结论限于所声明的干预机制。注意力图、IG图和遮挡检验不一致时必须讨论，不挑最好看的图。

### 6.7 输出与验收

`attachment4_predictions_explanations.csv`建议每样本一行，包含sample_id、极性、强度、三模态有符号贡献、归一化作用程度、主导模态、解释目标、映射状态。

`attachment4_evidence.csv`每证据区间一行，包含sample_id、modality、feature_start/end、word/character范围、start_sec、end_sec、attribution、mapping_method、mapping_status。没有可靠秒级映射时不填虚假秒数，保留空值和原因，并记录未通过定位要求。

每条样本生成可追溯解释记录；正文挑选典型解释卡，但20条全量预测与解释均须汇总。

**硬验收：**

- 20/20条样本有预测和解释，编号匹配视频；各贡献明确解释的是哪个固定目标。
- Shapley残差、7种非空联盟稳定性及空联盟定义有记录；采用IG时另记录固定掩码下的完整性残差和容差。残差小不替代忠实度验证；无主导模态情况得到正确处理。
- 证据不指向padding、特殊符号或视频外部；映射正确性有实际核验证据。
- 验证集和冻结后的官方test分开报告Accuracy、F1、MAE、Pearson及解释忠实度，不只展示案例图。
- 至少有独立扰动检验支持重要片段排序；解释未通过时不得称其可信。
- 若音视频特征无法可靠映射原素材，单独列为未完成项；不能以近似替换硬要求而不说明。

**内部采用目标：** 在验证阶段保持与问题二完整输入模型可比的预测性能，重要区间干预效应优于等长随机区间；不满足时可在该阶段改用更简单、可验证的方法。冻结后在test报告配对差异区间及失败情况；最终证据不足时收缩结论，不根据test返回选型。

## 7. 分阶段执行与验收门槛

截止时间和硬件条件尚未确认，以下按依赖顺序推进，不虚构训练耗时。

| 阶段 | 工作 | 进入下一阶段的条件 |
|---|---|---|
| S0 数据审计 | 复核字段、ID/标签、NaN/Inf、padding、重复与划分；查官方位置/时间映射依据 | 保存命令、哈希和检查输出；test仅做预定结构/完整性检查；列清G1/G2未知项，不读取专项内容补答案 |
| S1 环境与最小试验 | 硬件检查、问题一小样本试跑、文本/掩码检查、定位规则核验、依赖与试打包 | 按§2.4记录G1/G2/G3逐项证据；未通过的门槛阻止对应全量路线，允许独立工作继续 |
| S2 问题一全量 | 100条提取、对齐、人工检查、汇总 | 覆盖完整；异常透明；时间映射可复核 |
| S3 问题二基线 | B0/B1/B2，含B2相同缺失增强对照；固定指标和遮蔽清单 | 完整及缺失验证可复现；公平对照预算已记录 |
| S4 鲁棒模型 | R1固定/动态融合对照，多种子；按证据决定R2/R3 | 只用验证集选型；改进来源和局限可区分 |
| S5 问题三 | 模态Shapley＋局部遮挡；可选IG；联盟与忠实度验证 | G2已通过；离散掩码/归因基线定义清楚；验证阶段完成配置选择 |
| S6 冻结评估与专项推理 | 冻结权重、超参、阈值、解释/映射配置、种子与评估协议；官方test评估；专项推理与定位 | 验证/test分开记录且不回调；附件3全部30条、附件4全部20条输出并完成逐样本定位验收 |
| S7 论文与打包 | 整理真实结果，编译、匿名性、体积、复现检查 | 正文/CSV/图表一致，必需材料完整，压缩包≤50MB |

允许问题一与二的方法开发交替推进，因为二、三不依赖问题一生成的特征。G2未通过时可以进行不依赖该映射的基线研究，但第三问最终路线保持阻塞，不能把阶段编号前进当作验收通过。

### 7.1 时间不足时的取舍

保留：问题一100条覆盖、问题二基础对照与连续缺失实验、问题三真实可回看证据和忠实度检验、全部专项CSV、可复现代码与参数。

优先放弃：大型Transformer、生成式补全、扩散模型、大规模超参数搜索、可选IG及无验证增益的一致性/重建扩展、过多图形装饰。保留模态Shapley与局部遮挡的最低解释流程；不能放弃题目明确要求的定位、消融和全量输出。

### 7.2 计算和文件体积预算

- 先测单样本提取和一个训练epoch；由实测推算全量耗时，不按宣传页速度估算。
- 训练使用按需缓存与float32，避免同时持有约3GB非对齐数据的多份副本。
- 小型模型的双任务头和时序骨干以少量参数为目标；预训练BERT等权重通常远超50MB。
- 问题一保存全部自生成时序特征，不默认把中间视频、波形、可再生成大图全部打包。不能以压缩为由删掉要求提交的全量特征。
- 使用压缩NPZ并实际测体积；若考虑float16，必须验证数值误差、下游影响和读取支持，不能无验证降精度。
- 预训练依赖是否可凭下载脚本、精确版本和哈希复现，需查赛事统一规则或官方确认。若必须连同所有外部权重离线提交，就应提前改用满足体积限制的工具/模型，不能自作主张省略必需权重。
- 建议压缩包留余量，目标小于45MB；这是内部预算，不替代官方50MB上限。未澄清单位时按50,000,000字节保守检查，同时报告字节数、MB和MiB。

**G3在S1的最小交付：**

1. 列出软件、预训练权重、自训练权重、特征和说明材料的提交/外部获取方式；外部下载必须有规则依据、固定版本、哈希和可执行获取说明，不能只写“网上可下载”。
2. 对3—5条不同长度样本实际生成NPZ并测压缩体积；结合100条文本长度及特征维度估算全量体积与余量，不能只按最短样本外推。
3. 将拟用模型实际参数文件（可为初始化结构，仅作体积测量）、配置、代码与示例特征试打包；报告实际小包大小和预计全量总大小，不能把试打包当成最终包验收。
4. 若超预算或权重规则未明确，暂停受影响路线并调整。S2全量提取后复测特征体积，S7再验收实际最终包；早期估算不豁免最终50MB硬限制。

## 8. 目录、交付清单与论文组织

建议工作目录：

```text
E题/
├── 建模规划.md
├── code/                 # 读取、提取、训练、评估、解释、导出
├── configs/              # 工具版本、权重修订号、超参数与种子
├── problem1/             # 100条自生成特征与时间映射
├── models/               # 自训练参数与必要预处理参数
├── results/              # 分开的验证/test预测、指标、消融和专项CSV
├── figures/              # 真实实验图及绘图数据
├── reports/              # 数据审计、运行记录、验收问题清单
└── paper/                # 后续确定LaTeX或Typst及官方模板
```

原始数据仍在输入目录，不复制到提交包；用配置引用本地输入，不把个人绝对路径写入公开论文或提交日志。

### 8.1 给代码阶段的任务清单

| 任务 | 输入 | 输出 | 关键校验 |
|---|---|---|---|
| 数据审计与适配 | 四份附件 | 字段清单、标签映射、统一读取接口 | 三套结构：按split批量、单样本test、平铺单样本 |
| 文本路径验证（G1） | 附件2训练/验证原文与词元、专项结构、编码器配置 | 文本/掩码接口与检查输出 | tokenizer身份、掩码传播、占位值不变性、全缺失安全回退 |
| 定位规则核验（G2） | 官方说明/源码/答复、训练/验证位置结构 | token—word—音视频位置关系及时间依据 | 特殊符号、截断、padding、同步网格，不用专项内容选规则 |
| 早期体积核验（G3） | 提交规则、小样本特征、拟用模型与依赖 | 依赖清单、试打包记录、全量预算 | 权重是否须提交、外部获取依据、保守50MB预算 |
| 原视频提取 | 附件1视频/转写 | 全量时序NPZ、词时间戳、汇总表 | 100条覆盖、真实时间合法性、失败标记 |
| 基线与鲁棒训练 | 附件2train/valid | 模型、验证预测、缺失/消融指标 | train-only拟合、同等增强、固定/动态融合、多种子 |
| 可解释验证 | 验证集与候选模型 | Shapley/遮挡结果，可选IG | 联盟稳定性、基线和掩码固定、非循环扰动验证 |
| 冻结后测试 | 官方test、冻结模型与评估协议 | 分开保存的test预测、鲁棒性/忠实度指标及区间 | 不再选型、调阈值或挑种子；分组配对统计与泛化限制 |
| 专项最终推理 | 冻结模型、附件3/4 | 两类专项CSV和证据表 | 30/20条完整性、编号、数值和定位 |
| 提交打包 | 所有必需交付 | 压缩包和复现说明 | ≤50MB、匿名、干净环境可运行 |

### 8.2 每问论文必须出现的结果

- 第一问：处理流程、特征定义、对齐数学公式、100条汇总、典型样本时间对照、人工检查结果、异常与工具版本。
- 第二问：网络与目标函数、训练设置、单模态/简单融合对照、缺失类型/位置/长度分析、消融、错误归因、30条专项结果汇总。
- 第三问：解释目标和方法、验证集预测指标、归因忠实度/稳定性、模态贡献比较、片段证据图、20条专项预测与解释汇总。
- 第二、三问另列冻结后的官方test性能、鲁棒性和忠实度结果，与开发阶段验证集指标分表呈现；交代源视频重叠及推断范围。
- 摘要最后写，只引用已保存的真实指标并标明数据划分。无标签专项集不能报告准确率。
- 图表必须由真实结果生成。解释热图只是展示，不自动构成正确性证明。

## 9. 验收记录模板

每项验收填写“通过 / 未通过 / 待证据”，附实际文件和检查结果，不靠主观判断划勾。

| 验收项 | 当前状态 | 需要的证据 |
|---|---|---|
| 题面及输入结构确认 | 题面已读取；原稿数据记录待复核证据 | 题面来源、文件哈希、读取命令、两套主数据及专项字段检查输出 |
| 资料能力与许可核验 | 待证据 | 访问日期、版本/提交号、关键结论来源及本地试验 |
| 数据全量质量/重复/标签一致性 | 待证据 | audit报告及异常清单 |
| G1 文本编码器与掩码接口 | 待证据 | tokenizer核验、同路径推理、掩码传播及占位不变性检查 |
| G2 特征位置与原素材定位规则 | 待证据，阻塞第三问最终路线选定 | 官方依据、跨模态网格/词元映射、截断填充规则及可核查记录 |
| G3 依赖与提交体积 | 待证据 | 外部权重规则、依赖清单、S1试打包及全量预算 |
| 第一问全量特征与时间对齐 | 待执行 | 100条特征、manifest、人工核验 |
| 第二问训练和鲁棒性 | 待执行 | 指标、预测、遮蔽清单、多种子与消融 |
| 第三问解释与原素材映射 | 待执行，须先通过G2 | 联盟稳定性、归因验证、时间来源及全量逐样本回看证据 |
| 冻结与官方test评估 | 待执行 | 配置/权重哈希、预登记协议、独立保存的预测和区间 |
| 30/20条专项结果 | 待执行 | CSV完整性检查及定位状态 |
| 复现、匿名性、≤50MB | 待执行 | 重跑记录、包内容、大小检查 |

当前不承诺任何预测精度，也不把本规划视为建模完成。最先实施S0/S1，发现假设错误时更新方案，并保留修改原因。

本次规划修订记录：前移G1/G2/G3门槛；补齐掩码传播、IG条件基线及Shapley联盟验证；增加公平对照和冻结后test评估；允许两问共享权重；将缺少可查记录的历史数据/联网结论标为待复核。以上均为方案修改，未执行数据核验、提取、训练或专项推理。

## 10. 参考入口与待补核验记录

以下为原稿记载的查阅入口，本次仅保留链接，未复核其访问记录或当前内容。工具能力与许可摘要见第3节；实施时补齐访问日期、版本/提交号和支持具体结论的页面位置，核验后再更新验收状态。

1. Montreal Forced Aligner：Align with an acoustic model。https://montreal-forced-aligner.readthedocs.io/en/latest/user_guide/workflows/alignment.html
2. WhisperX官方仓库。https://github.com/m-bain/whisperX
3. WhisperX已知转写对齐接口源码。https://raw.githubusercontent.com/m-bain/whisperX/main/whisperx/alignment.py
4. BERT base uncased模型卡。https://huggingface.co/google-bert/bert-base-uncased
5. openSMILE Python官方文档，含LLD/Functionals维数与许可证说明。https://audeering.github.io/opensmile-python/
6. OpenFace官方仓库。https://github.com/TadasBaltrusaitis/OpenFace
7. MediaPipe Face Landmarker官方指南。https://ai.google.dev/edge/mediapipe/solutions/vision/face_landmarker
8. CMU Multimodal SDK，时间区间与参考模态对齐说明。https://github.com/CMU-MultiComp-Lab/CMU-MultimodalSDK
9. Tsai et al. Multimodal Transformer for Unaligned Multimodal Language Sequences. ACL, 2019。https://aclanthology.org/P19-1656/
10. Serrano and Smith. Is Attention Interpretable? ACL, 2019。https://aclanthology.org/P19-1282/
11. Sundararajan, Taly and Yan. Axiomatic Attribution for Deep Networks. ICML, 2017。https://proceedings.mlr.press/v70/sundararajan17a.html
12. Captum Integrated Gradients官方文档。https://captum.ai/docs/extension/integrated_gradients
13. SHAP ExactExplainer官方接口。https://shap.readthedocs.io/en/latest/generated/shap.ExactExplainer.html

这些资料支持方法选择，不表示其发布的性能适用于本题4850条子集。引用前需核对原文；不能把官方完整MOSEI的基准成绩当成本题实验成绩。
