# 1 问题重述

## 1.1 问题背景

在人机交互中，仅凭转写文本难以完整判断说话人的情感。同一句话可以因语调、语速或面部表情不同而呈现不同的情感倾向。文本、语音与视觉分别提供语义、韵律和表情等信息，多模态情感预测需要利用这些信息的关联与差异，判断视频片段的情感极性及强度。

多模态信息的联合使用面临三个具体问题。一是各模态的采样频率和时间粒度不同，文本以词或子词组织，音频与视频则按各自的采样规则记录，必须建立可核验的时间对应关系。二是噪声、遮挡或转写失败可能使某一模态的局部连续时段不可用，模型需要利用剩余信息完成预测。三是预测结果本身不足以说明模型的判断依据，还需量化不同模态的作用，并将关键证据对应至原始文本、语音时段或视频帧。

本题以赛题提供的 CMU-MOSEI 英文多模态数据为研究对象，围绕特征提取与时序对齐、局部模态缺失下的鲁棒预测以及预测结果的可解释性开展建模。

## 1.2 数据介绍与建模目标

### 1.2.1 数据介绍

赛题提供四类附件，其内容与用途如下。

| 数据附件 | 主要内容 | 对应任务 |
| --- | --- | --- |
| 附件1 | 100条英文原始视频及转写文本、情感标签，来源于37个原始视频目录，片段时长为2.648～34.567秒 | 自主提取三模态特征，建立并验证时间对应关系 |
| 附件2 | 对齐和非对齐两套标准化多模态特征，每套包含4850条样本，按训练、验证和测试用途划分 | 问题二、三的模型训练、选择与性能评价 |
| 附件3 | 局部连续区间存在模态缺失的无标签特征样本 | 鲁棒预测模型的专项推理 |
| 附件4 | 三模态信息完整的无标签特征样本及配套原始视频 | 可解释模型的专项推理与原素材证据核查 |

附件1以原始视频标识和片段编号共同确定样本。情感强度标签取值为 $[-3,3]$：负值表示负向情感，零表示中性，正值表示正向情感，绝对值反映标注的情感强度。零仅属于中性类别。

附件2的对齐版将三模态组织为最多50个相互对应的序列位置，文本、语音和视觉特征维度分别为768、74和35。非对齐版的文本仍保留最多50个位置，语音和视觉各保留最多500个位置，并提供相应有效长度。固定的存储长度不等于样本的实际有效长度，填充位置需与有效观测区分。预计算文本特征与 BERT 三路输入是同一文本模态的两种表示，不能作为两个独立模态重复计入。

附件3、4分别提供与附件2对应的两套版本。问题二、三须在各自的训练、验证和专项推理过程中保持特征版本及模型输入接口一致。所有训练与评价数据均限定为赛题提供的数据，不引入额外情感数据集。

### 1.2.2 建模目标

问题一的目标是将原始视频及转写文本转化为具有明确时间含义的三模态特征序列。生成结果应覆盖全部100条样本，记录样本编号、特征维度、有效长度、时间区间及异常状态，使序列中的有效特征能够追溯至原始素材。

问题二的目标是在一个或多个模态的局部连续信息缺失时，仍能输出情感极性和情感强度，并分析缺失模态类型、位置、时长及缺失率对性能的影响。分类性能采用准确率和 F1 值评价，回归性能采用平均绝对误差和皮尔逊相关系数评价。

问题三的目标是在给出情感预测的同时，量化各模态对当前预测的作用，识别主要参考模态及其中的关键局部片段，并将证据定位至可回看的原始素材。解释的有效性需要通过输入扰动等检验支持，不能仅以可视化效果作为依据。

## 1.3 问题提出

结合数据条件与任务要求，需要解决以下三个问题。

**问题一：如何构建可追溯的三模态时序表示？** 基于附件1，定义文本、语音和视觉特征，建立不同采样粒度之间的时间对应规则，说明提取、对齐及异常处理过程。除提交全部样本的特征与汇总信息外，还需通过至少一个典型样本展示文本、语音、视频帧与特征的对应关系。

**问题二：如何利用局部不完整的信息进行情感预测？** 基于附件2训练集与验证集，建立兼顾极性分类和强度回归的鲁棒模型。通过可控的连续区间缺失实验与消融对照，研究不同缺失条件下的性能变化，并对附件3全部样本输出预测。局部缺失不能简单等同于整个模态不存在。

**问题三：如何解释预测结果并定位判断依据？** 基于附件2建立可解释预测模型，量化三模态的作用差异，识别影响预测的关键片段，检验解释是否反映模型实际使用的信息。对附件4输出预测与解释，并核查关键证据与原始文本、语音和视频的对应关系。

三问在研究内容上递进，但数据用途不同。问题一负责自主特征构建，问题二、三可以直接使用附件2开展建模，不要求以问题一的100条样本作为预测模型训练集。

## 1.4 解题流程

整体研究按数据核查、分问建模、实验验证和结果整理展开。

数据核查阶段确认样本编号、标签定义、字段类型、有效长度及官方数据划分，区分填充、原生无效观测与人工缺失。模型参数仅在附件2训练集上学习，结构、超参数和阈值在验证集上选择；专项测试集仅用于最终推理。

问题一以视频片段内的时间为统一参照，利用已知转写与音频建立词级时间区间，分别提取文本表示、声学描述子和视觉特征，再按时间重叠关系聚合音视频特征。通过小样本核查处理规则后，完成全部样本的提取，并记录异常与对齐质量。

问题二从简单预测基线出发，引入显式缺失掩码及连续区间遮蔽训练，研究剩余模态与邻近时序信息的利用方式。在一致的评价条件下比较模型，并开展缺失类型、位置和程度的对照实验。

问题三在预测模型基础上分析模态级贡献与局部片段重要性，通过遮蔽或移除关键输入检查预测变化。原素材定位须以可验证的时间映射为依据，不将序列位置的均匀缩放视为真实时间戳。

模型与评价方案确定后，开展最终测试和专项推理，整理全量特征、预测结果、解释结果及复现材料。论文中的性能结论以实际实验为依据，提交材料同时满足匿名性及总附件不超过50 MB的要求。

# 2 模型假设

为明确模型的适用范围，作如下初步假设。各假设需在相应模型建立与实验分析中检验；不满足假设的样本应保留并标记，不能通过删除样本掩盖处理失败。

**假设一：有效语音片段中的转写文本与实际语音在词序上基本一致。**

该假设使已知转写的强制对齐具有依据，不要求每个词都能获得准确边界。对于漏词、非语音内容或明显不匹配部分，记录对齐失败或不确定状态，不赋予未经核验的时间位置。该假设主要服务于问题一，不能用于推定附件2历史特征的生成规则。

**假设二：经媒体时间信息核查与必要的偏移校正后，同一片段的音频和视频能够使用统一的片段内时间轴。**

该假设用于建立跨模态时间对应关系，不要求音视频采样率相同，也不要求视频帧率恒定。若存在无法校正的时间偏移，则该片段的精确对齐结果不予确认，并单独报告其影响。

**假设三：在选定的短时间区间内，有效声学和视觉描述子的时间加权汇总可作为该区间的近似表示。**

该假设允许将不同采样频率的特征聚合到共同时间粒度，以控制序列长度和建模复杂度。聚合可能削弱瞬时变化，因此需通过典型片段检查及不同聚合粒度的对照评价其影响；不假设区间内情感或特征严格恒定。

**假设四：用于模拟观测缺失的人工遮蔽只改变模型可见的信息，不改变原始片段对应的情感标签。**

该假设用于定义问题二的缺失增强与鲁棒性评价：模型在不完整观测下仍预测原始片段的情感。遮蔽后的信息未必足以唯一确定标签，性能下降属于需要分析的结果。随机遮蔽也不被视为所有真实故障机制的完整复现。

**假设五：在相同特征处理规则下，附件2中可学习的特征与情感之间的统计关系，对赛题专项样本具有一定适用性。**

该假设是利用训练数据预测未标注样本的前提，不等于训练集与专项测试集的分布完全相同。其合理性需通过验证集、保留测试集及不同缺失条件下的性能检验间接评估；无标签专项结果本身不能证明泛化准确率。

上述假设之外，标签取值范围、官方数据划分、禁止额外情感数据参与训练及专项测试集的使用限制，均属于赛题规定，不作为模型假设重复列出。模型归因反映的是给定模型和解释规则下的预测依据，不直接等同于人类情感形成的因果机制。
