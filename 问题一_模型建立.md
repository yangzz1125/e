# 3 问题一：多模态特征提取与时序对齐

## 3.1 问题分析

附件1提供原始视频及对应的英文转写，三类信息在时间组织上存在差异：文本由词及子词构成，语音特征由短时分析窗产生，视觉特征则依附于视频帧。直接将三类特征按序号拼接，无法保证同一位置描述的是同一时段；将整段视频压缩为单个向量，又会丢失局部变化及其与原素材的对应关系。因此，问题一需要同时解决特征表示与时间对应两个问题。

以词为对齐单元，可以保留文本的语义边界，并为语音与视觉提供共同的聚合区间。依据假设一和假设二，采用已知转写与音频的强制对齐获得词的起止时间，再将音视频特征转换到统一的片段内时间轴。依据假设三，通过时间重叠加权，将不同采样频率的描述子汇总为词级表示。

据此，建立“词级时间锚定的三模态特征表示模型”。模型输出包括文本、语音和视觉特征矩阵，以及词时间区间、观测掩码和质量记录。这里的时间对应表示各模态描述同一物理时段，不假定表情、语调与词义之间不存在反应滞后，也不将时间同步视为语义完全一致。

本问以特征覆盖、时间可核验性和处理可复现性为主要目标，不以情感预测准确率作为特征提取是否成功的判断标准。情感标签仅随样本保留，不参与词边界估计或特征筛选。问题一自主定义的特征也不要求与附件2的特征维度一致。

## 3.2 符号与变量定义

设附件1包含 $N=100$ 条样本，第 $i$ 条样本表示为

$$
\mathcal S_i=(v_i,a_i,r_i,y_i),\qquad i=1,\ldots,N,
$$

其中，$v_i$ 为视频帧序列，$a_i$ 为音频信号，$r_i$ 为原始转写，$y_i$ 为赛题提供的情感强度标签。以视频片段起点作为时间原点，时间单位统一为秒。主要符号见下表。

| 符号 | 含义 |
| --- | --- |
| $D_i$ | 第 $i$ 条视频的时长，单位为秒 |
| $K_i$ | 规范化转写中的词单元数量，不包括特殊词元与批处理填充 |
| $c_{ik}$ | 第 $k$ 个词单元对应的原文字符范围或范围集合 |
| $I_{ik}=[s_{ik},e_{ik})$ | 第 $k$ 个词的时间区间，边界单位为秒 |
| $b_{ik}$ | 词时间映射可用标记，取值为0或1 |
| $m\in\{T,A,V\}$ | 文本、语音、视觉模态标识 |
| $J_{ij}^{(m)}$ | 音频或视频第 $j$ 个描述子的时间支持区间 |
| $\mathbf u_{ij}^{(m)}$ | 第 $j$ 个帧级描述子，维度为 $d_m$ |
| $q_{ij}^{(m)}$ | 帧级描述子有效标记，取值为0或1 |
| $\mathbf x_{ik}^{(m)}$ | 第 $k$ 个词对应的模态特征向量 |
| $o_{ik}^{(m)}$ | 词级模态特征可用标记，取值为0或1 |
| $p_{ik}$ | 批处理时的真实词位置标记，真实词为1，填充位置为0 |

对于具有可用时间映射的词，要求

$$
0\le s_{ik}<e_{ik}\le D_i.
$$

同一说话序列中，已对齐词的时间顺序应与词序一致。没有可靠边界的词仍保留其文本和序列位置，但设置 $b_{ik}=0$，时间字段记录为空，不使用零长度伪区间代替失败结果。

## 3.3 三模态特征提取模型

### 3.3.1 原始素材处理与时间基准

通过样本的原始视频标识和片段编号，将视频与转写逐条匹配。媒体解析保留音视频流的起始时间、视频帧显示时间戳、音频采样率和时长。设某一流中事件的局部时间为 $t_{\mathrm{local}}$，该流起点相对于片段起点的偏移为 $\Delta_i^{(m)}$，则统一时间为

$$
t=t_{\mathrm{local}}+\Delta_i^{(m)}.
$$

音轨解码为单声道波形，并按声学模型要求重采样；本方案以16 kHz作为强制对齐输入采样率。重采样只改变离散采样网格，不改变片段的物理时间含义。音频采样点 $n$ 的时间由 $n/f_s+\Delta_i^{(A)}$ 确定，其中 $f_s$ 为重采样后的采样率。声学特征使用的分析窗和步长随配置保存。

视频保留逐帧时间戳，不以帧号除以名义帧率替代可变帧率素材的真实时间。若提取工具重新生成了时间序列，须通过解码帧索引映射回原始时间戳，并检查是否存在丢帧、重复帧或顺序变化。

### 3.3.2 文本语义特征

文本采用冻结的 `google-bert/bert-base-uncased` 编码器，提取最后一层的上下文表示，不在附件1上进行情感任务微调。该模型提供768维词元表示，适合处理本题英文转写。

原始转写保持不变，另建立用于分词与强制对齐的规范化副本。大小写、标点、缩写和数字转换均保存到原文的映射关系。由于一个原始词可能被拆分为多个规范化单元，字符映射允许一对多关系，不能仅按字符位置直接复制索引。

设编码后的第 $\ell$ 个子词表示为 $\mathbf h_{i\ell}\in\mathbb R^{768}$，映射到第 $k$ 个词的子词集合为 $\mathcal B_{ik}$。当集合非空且编码结果有效时，词级文本表示定义为

$$
\mathbf x_{ik}^{(T)}=
\frac{1}{|\mathcal B_{ik}|}
\sum_{\ell\in\mathcal B_{ik}}\mathbf h_{i\ell}.
$$

子词归属通过分词器返回的字符偏移与规范化词边界确定。特殊词元和填充词元不参与均值计算，无法建立可靠字符映射的词设置文本无效标记。未知词元单独记录，不将其视为原文词义已被完整识别。

对超过编码器输入长度限制的文本，在词边界处分块，保留块编号及块内到全局的索引映射。全部词均需进入处理记录，不沿用附件2最多50个位置的限制截断附件1。分块会限制跨块上下文，因此在处理说明中记录分块规则与边界位置。

文本语义特征可用与词时间边界可用是两个不同条件。一个词即使未成功对齐，也可能具有有效文本表示，此时 $o_{ik}^{(T)}=1$、$b_{ik}=0$，不能将它当作已具有三模态时间对应关系的词。

### 3.3.3 语音声学特征

采用 openSMILE 的 eGeMAPSv02 低层描述子构建声学序列，声学维度为 $d_A=25$。选择帧级描述子而非整段88维统计量，是为了保留局部时序变化和词区间内的聚合依据。

第 $j$ 个声学向量记为

$$
\mathbf u_{ij}^{(A)}=
\bigl[u_{ij,1}^{(A)},\ldots,u_{ij,25}^{(A)}\bigr]^{\mathsf T}.
$$

每一维的字段名、量纲及工具配置随特征保存。时间支持区间由提取器返回的起止时间转换到统一时间轴获得，不自行假设所有描述子都来自相同长度的分析窗。所选25维特征是本问自主定义的声学表示，不等同于附件2的74维语音特征。

声学描述子有效性根据音频解码状态、时间范围及数值有限性判定。无声区间与解码失败分别记录：无声并不必然表示观测缺失，工具按定义输出的零值也不直接判为无效。若某些声学量在无声或非浊音条件下有特殊取值，其含义依据配置解释。

### 3.3.4 视觉行为特征

视觉模态采用 OpenFace 提取面部动作单元强度、头部姿态和视线信息。动作单元描述局部面部运动，头姿与视线提供头部朝向及注视方向的补充信息；这些描述子本身不直接等同于情感类别。

将选定字段组成帧级向量

$$
\mathbf u_{ij}^{(V)}=
\left[
(\mathbf r_{ij}^{\mathrm{AU}})^{\mathsf T},
(\boldsymbol\theta_{ij})^{\mathsf T},
(\mathbf g_{ij})^{\mathsf T}
\right]^{\mathsf T},
$$

其中，$\mathbf r_{ij}^{\mathrm{AU}}$ 为动作单元强度向量，$\boldsymbol\theta_{ij}$ 为三维头部旋转角，$\mathbf g_{ij}$ 为所选视线描述向量，$d_V$ 等于三部分维度之和。字段顺序、单位和维度以固定版本的实际输出清单为准，在全量提取前锁定；不根据附件2的35维输入反推本问的视觉字段。

为避免检测失败产生的数值进入聚合，设置

$$
q_{ij}^{(V)}=
\mathbb 1\!\left\{
\mathrm{success}_{ij}=1,
\ \mathrm{confidence}_{ij}\ge\tau_V,
\ \mathrm{finite}(\mathbf u_{ij}^{(V)})
\right\},
$$

其中，$\tau_V$ 为预先确定的质量阈值，$\mathbb 1\{\cdot\}$ 为条件成立时取1的指示函数。阈值依据小样本人工核查确定并记录，同时保留连续置信度用于敏感性分析，不将其解释为已经校准的正确检测概率。

同一片段须保持主体身份一致。出现多个人脸时，结合转写对应的说话人核查主体，并保留身份跟踪记录；主体无法确认的片段标记为歧义，不逐帧选取面积最大的人脸拼成一条序列。主方案不因个别样本失败而混入另一工具的不同维度特征。

## 3.4 词级时序对齐模型

### 3.4.1 基于已知转写的词边界估计

使用 Montreal Forced Aligner（MFA），以音频、规范化转写、发音词典和预训练声学模型为输入，获得词级起止时间。该过程利用题目已提供的转写，不以自动语音识别结果替换原文。[MFA 强制对齐说明](https://montreal-forced-aligner.readthedocs.io/en/latest/user_guide/workflows/alignment.html)

令 $\mathcal A(r_i)$ 表示与转写相容、保持词序并允许停顿的候选对齐路径集合，$\gamma$ 为其中一条声学对齐路径。强制对齐的统计目标可概括为

$$
\widehat\gamma_i=
\underset{\gamma\in\mathcal A(r_i)}{\arg\max}\;
\log P(a_i,\gamma\mid r_i,\Theta_{\mathrm{align}}),
$$

其中，$\Theta_{\mathrm{align}}$ 为对齐器的模型参数与配置。由路径中的词边界得到 $\widehat I_{ik}$。此式用于说明条件对齐的目标，不表示重新训练声学模型，也不保证实际搜索取得无误差的全局最优边界。

边界输出需满足时间范围、正时长和词序一致性条件。检查通过说明其结构可用，并不证明时间位置准确；准确性仍需通过音频回听和人工边界核查评估。停顿保留在时间轴中，不强行分配给邻近词。对齐器无法识别的词保留失败状态，不将整段时长均匀分配给各词。

### 3.4.2 音视频描述子的时间支持区间

声学描述子直接使用提取器提供的时间区间。对于仅有帧时间戳的视频描述子，设经核查的相邻帧时间为 $t_{i,j-1},t_{ij},t_{i,j+1}$，内部帧的支持区间定义为

$$
J_{ij}^{(V)}=
\left[
\frac{t_{i,j-1}+t_{ij}}{2},
\frac{t_{ij}+t_{i,j+1}}{2}
\right)\cap[0,D_i].
$$

首尾帧依据实际流起止时间及可用帧持续时间确定外侧边界。若存在明显断帧或时间戳跳变，先划分连续帧段，再在各段内构造支持区间，不将长时间空缺交给相邻帧覆盖。该区间是离散视频帧参与聚合的时间近似，不意味着一个画面状态在整个区间内完全不变。

### 3.4.3 时间重叠加权聚合

对于 $b_{ik}=1$ 的词以及模态 $m\in\{A,V\}$，记 $J_{ij}^{(m)}=[\alpha_{ij}^{(m)},\beta_{ij}^{(m)})$，定义重叠长度

$$
\ell_{ikj}^{(m)}=
\max\!\left\{0,
\min(e_{ik},\beta_{ij}^{(m)})-
\max(s_{ik},\alpha_{ij}^{(m)})
\right\}.
$$

有效权重与权重和分别为

$$
w_{ikj}^{(m)}=q_{ij}^{(m)}\ell_{ikj}^{(m)},
\qquad Z_{ik}^{(m)}=\sum_j w_{ikj}^{(m)}.
$$

词级音视频特征定义为

$$
\mathbf x_{ik}^{(m)}=
\begin{cases}
\displaystyle\frac{\sum_{j:q_{ij}^{(m)}=1}
\ell_{ikj}^{(m)}\mathbf u_{ij}^{(m)}}{Z_{ik}^{(m)}},
&Z_{ik}^{(m)}>0,\\[6pt]
\mathbf 0,&Z_{ik}^{(m)}=0.
\end{cases}
$$

计算时先剔除无效向量再求和，避免以零乘非有限数的方式传播异常。当词边界不可用时，不计算重叠量，直接设置音视频词级占位向量及无效标记。统一写为

$$
o_{ik}^{(m)}=
\begin{cases}
1,&b_{ik}=1\ \text{且}\ Z_{ik}^{(m)}>0,\\
0,&\text{其他情况},
\end{cases}
\qquad m\in\{A,V\}.
$$

该聚合也可解释为词区间内有效描述子的加权最小二乘代表值。当 $Z_{ik}^{(m)}>0$ 时，

$$
\mathbf x_{ik}^{(m)}=
\underset{\mathbf z\in\mathbb R^{d_m}}{\arg\min}
\sum_{j:q_{ij}^{(m)}=1}
\ell_{ikj}^{(m)}\|\mathbf z-\mathbf u_{ij}^{(m)}\|_2^2.
$$

对 $\mathbf z$ 求导并令其为零，即得到上述加权均值。该目标逐维计算同一描述子的区间代表值，不用于比较不同量纲特征的重要性。重叠越长的帧对代表值贡献越大，但权重只描述时间关联，不表示情感重要程度。

### 3.4.4 有效覆盖与掩码

仅有少量有效帧与词区间相交，也可能产生非零权重和。因此，除二元可用标记外，还定义区间覆盖率

$$
\rho_{ik}^{(m)}=
\frac{
\left|I_{ik}\cap\displaystyle\bigcup_{j:q_{ij}^{(m)}=1}J_{ij}^{(m)}\right|
}{e_{ik}-s_{ik}},
\qquad b_{ik}=1,
$$

其中 $|\cdot|$ 表示时间长度，$0\le\rho_{ik}^{(m)}\le1$。计算覆盖率使用有效区间的并集，避免重叠声学窗被重复计数；不能直接以 $Z_{ik}^{(m)}/(e_{ik}-s_{ik})$ 代替。无可靠词边界时覆盖率记为未定义，并由 $b_{ik}$ 标识。

每条样本保存变长矩阵

$$
\mathbf X_i^{(T)}\in\mathbb R^{K_i\times768},\quad
\mathbf X_i^{(A)}\in\mathbb R^{K_i\times25},\quad
\mathbf X_i^{(V)}\in\mathbb R^{K_i\times d_V}.
$$

三者按同一词序组织，但是否具有有效数值和时间对应关系由掩码决定。后续批处理若需扩展到长度 $K_{\max}$，则令 $p_{ik}=\mathbb 1\{k\le K_i\}$。填充位置、真实词的观测失败以及时间对齐失败分别保存，不通过向量是否全零推断状态。

词级矩阵只描述词区间及其上下文表示，不穷尽停顿中的表情或其他非语言事件。因此，停顿与未对齐时段另行登记，并保留帧级提取结果及原素材索引，以支持对这些时段的回看。

## 3.5 异常处理、输出规范与质量检验

### 3.5.1 异常处理规则

处理过程保留全部100条样本。对每条样本分别记录媒体解析、文本编码、强制对齐、声学提取和人脸分析的状态，不以单一的“成功”标记覆盖各环节差异。

| 异常情形 | 处理规则 |
| --- | --- |
| 词边界缺失或不合法 | 保留词及可用文本特征，设置时间无效标记；音视频词级特征不冒充对齐成功 |
| 无声、背景音乐或转写不匹配 | 区分声学观测可用性与词对齐可用性，保留音频记录并核查对应关系 |
| 无人脸、低置信度或主体不明确 | 标记相应视觉帧无效，保留其他模态与失败原因 |
| 描述子含 NaN 或 Inf | 将受影响的描述子标记无效，数值输出使用显式占位并记录异常位置 |
| 局部断帧或流时间偏移 | 核查时间映射，保留空缺区间；未解决前不声明精确跨模态对应 |
| 文本超过编码长度限制 | 按词边界分块，保存映射，不删除尾部文本 |

确需人工修正或局部插值的边界，另存修正值、修正依据与标记，不覆盖原始素材及原始转写。插值结果与直接估计结果分开统计，不使用情感标签选择边界。

### 3.5.2 特征文件组织与复现信息

每条样本的输出由特征数组和对应元数据组成。特征数组采用可无损压缩的数值格式，元数据保留编号、文本映射和时间依据；连续特征可采用 float32，时间字段保留足够精度，并在保存后检查数值及时间误差。

| 输出内容 | 必要字段 |
| --- | --- |
| 样本信息 | 原始视频标识、片段编号、文件哈希、原始时长及原标签 |
| 三模态特征 | 三个词级矩阵、维度、字段顺序、实际词数 |
| 时间与文本映射 | 原文字符范围、规范化词、子词索引、词起止时间、帧索引及时间支持区间 |
| 状态信息 | 词对齐标记、各模态观测掩码、覆盖率、异常与修正记录 |
| 复现信息 | 工具版本、模型权重版本、完整参数、字段清单和运行说明 |

汇总表须逐条覆盖100个样本，并分模态报告原始时长、有效覆盖时长、特征维度、序列长度和对齐粒度。有效覆盖时长按有效区间并集计算，不以有效帧数乘名义帧间隔替代。样本已有输出记录与模态提取成功分别统计。

### 3.5.3 模型质量的检验口径

样本覆盖率定义为

$$
C_{\mathrm{sample}}=
\frac{\text{具有完整处理记录的样本数}}{100}.
$$

该指标检查是否遗漏样本，达到1并不表示所有模态均提取成功。词时间映射可用率定义为

$$
C_{\mathrm{align}}=
\frac{\sum_i\sum_{k=1}^{K_i}b_{ik}}{\sum_i K_i},
$$

各模态词级可用率以同一词总数为分母，分别统计 $o_{ik}^{(m)}$。同时报告覆盖率分布及失败原因，避免仅有很短有效观测的词与完整覆盖的词被视为相同质量。上述比例均不能代替对齐准确性检验。

对人工复核子集，设第 $k$ 个词的参考起止时间为 $s_{ik}^{*}$、$e_{ik}^{*}$，起止边界绝对误差分别为

$$
\varepsilon_{ik}^{s}=|s_{ik}-s_{ik}^{*}|,
\qquad
\varepsilon_{ik}^{e}=|e_{ik}-e_{ik}^{*}|.
$$

起点与终点分别报告误差中位数、90%分位数和对应样本数，并说明人工标注规则及复核范围。复核子集覆盖不同视频长度和观测质量，未成功对齐的词另报失败率，不从统计说明中隐去。没有人工参考边界时，只报告时间合法性、覆盖情况和人工回看记录，不计算所谓真实边界误差。

典型样本展示以同一时间轴并列呈现原文词序、语音波形、视频关键帧及相应特征。通过改变聚合粒度、视觉置信度阈值和小幅扰动词边界，考察覆盖率与特征变化，检验假设二、三及处理参数对输出的影响。词级聚合的可读性与固定时间窗方案进行对照，其效果由实际结果判断。

## 3.6 模型求解与结果分析

> 本节待真实特征提取及人工核验完成后撰写。需包含全部100条样本的汇总表、至少一个真实样本的对齐展示、各模态可用性与边界误差统计、异常样本分析及参数敏感性结果。当前章节仅完成模型建立，不代表已经获得上述实验结果。
