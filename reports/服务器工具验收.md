# 服务器工具验收

日期：2026-09-23。工作目录：`/hy-tmp/E`。本次只处理工具、模型和独立测试文件，没有操作用户上传中的题目数据。

| 项目 | 验证结果 |
|---|---|
| Python 模型环境 | Transformers 4.44.2、Captum 0.9.0、openSMILE 2.6.0 等已安装，RTX 3090 Ti CUDA 运算、反向传播、优化器更新通过 |
| FFmpeg / ffprobe | 4.4.2，已解码 OpenFace 官方示例视频并导出 2 秒测试片段 |
| MediaPipe | 补齐 EGL/GLES 运行库后，人脸模型加载和空白图推理通过 |
| BERT base uncased | 固定 revision 的 safetensors 与官方哈希一致；服务器离线 GPU 前向输出 `(1, 14, 768)` |
| OpenFace + dlib 19.24 | 编译成功；CLNF 模式处理 30 帧，30 帧均成功，输出 470 列，关键点、头姿、视线、AU 抽查列均有限 |
| OpenFace CE-CLM | 四个模型已部署、两端 SHA256 一致；默认模式 30 帧均成功，输出 470 列，关键数值有限，退出码 0 |
| MFA | 3.3.10，独立 Conda 环境、CPU Kaldi；英文声学模型与词典 v3.0.0 已就绪 |
| 强制对齐功能 | espeak-ng 生成的测试语音输出 TextGrid，8 个单词内容一致、区间有序且处于合法时间范围 |

MFA 测试词时间区间：this 0.04–0.24、is 0.24–0.37、a 0.37–0.43、simple 0.47–0.71、test 0.95–1.33、of 1.33–1.47、speech 1.59–1.76、alignment 1.90–2.48（秒）。这只是运行测试，不是题目数据的对齐精度评估。

服务器证据：

- `reports/full-server-check.txt`：完整基础环境检查通过，退出码 0。
- `reports/bert-test.log`、`reports/bert-test.exit`：预训练 BERT 推理通过。
- `reports/openface-native-build.log`：dlib、OpenFace 编译记录。
- `reports/openface-clnf-test.log`、`reports/openface-smoke/clnf/clip.csv`：CLNF 视频测试。
- `reports/openface-ceclm-test.log`、`reports/openface-smoke/ceclm/clip.csv`：默认 CE-CLM 视频测试。
- `reports/mfa-smoke.log`、`reports/mfa-smoke/aligned/speaker/sample.TextGrid`：合成语音对齐记录。
- `requirements-server.local.lock.txt`、`requirements-mfa-linux.explicit.txt`、`reports/native-packages.txt`：Python、Conda、系统工具版本清单。

运行入口为 `source /hy-tmp/E/scripts/activate-server.sh`，同时提供项目 Python 和隔离的 `mfa`、`FeatureExtraction` 命令。具体路径见 `README-server.md`，来源与哈希见 `reports/模型来源.md`。

仍需在题目数据上传完成后验证：tokenizer 是否匹配附件编号、真实视频的人脸成功率、逐词时间边界、无声/无人脸情况的掩码处理。尚未启动正式特征提取或模型训练。
