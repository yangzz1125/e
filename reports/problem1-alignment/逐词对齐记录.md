# 问题一：逐词对齐进度（未完成验收）

> **隔离重试更新：** 初次100条MFA为79通过、15待复核、6无TextGrid；6条另在独立目录用MFA `--beam 100 --retry_beam 400 --num_jobs 1`重新对齐，得到4通过、2待复核（波形过于安静，无法验证偏移），0缺失。未覆盖初次运行；可追溯合并后的`alignment-effective.json`共**83条自动通过、17条待复核、0条缺失**。自动通过不等于人工确认边界。

代码：`code/problem1/align_words.py`。输入：附件1的原始MP4和转写，`problem1/media/manifest.csv`及`transcript-segments.json`；不修改输入。服务器输出：`/hy-tmp/E/problem1/alignment/all/alignment.json`、`textgrids/`、`mfa.log`；本地保留`alignment.json`。MFA在题目MP4的**编辑列表选中音频**上生成词时间，按**音频轨道**`media_time / media_timescale`移到完整原片音频时间轴；没有题面对应转写的前段仍保留音视频，不伪造文本词。

| 状态 | 数量 | 解释 |
|---|---:|---|
| `pass` | 79 | 有TextGrid、MFA词序与简单原文分词一致、局部时间不越界、剪辑/全片音频波形匹配 |
| `needs_review` | 15 | 13条原文缩写/标记/分词与MFA输出不完全一致；2条音频相关性低于0.9，但偏移起点误差仍小于0.00003秒。需逐条核查，不能自动判定为错误或成功 |
| `error` | 6 | MFA运行返回成功但未生成该条的TextGrid；仍保留原片、原文和错误信息，不用插值造词区间 |

初次六条缺少TextGrid的ID：`-NFrJFQijFE__1`、`-NFrJFQijFE__2`、`-mJ2ud6oKI8__1`、`-mJ2ud6oKI8__2`、`-ri04Z7vwnc__2`、`-yRb-Jum7EQ__5`。初次MFA日志显示部分Kaldi任务有对齐错误；仅用6条重跑且使用更宽束宽后全部产生TextGrid，但这只解决了**缺失输出**，不证明声学边界正确。`-mJ2ud6oKI8__1`和`-mJ2ud6oKI8__2`音频片段过于安静，波形核验仍不可用，保持待复核。不将“工具调用成功”当作全量成功。

3条事先选择的短/中/长样本用完整WAV与剪辑WAV做独立波形核验，编辑列表起点与波形估计差约0.00002～0.00003秒、归一化相关性约0.994～0.996；三条MFA词数分别5、9、49且自动检查通过。**这只验证时间轴换算和程序输出，仍未人工回听词边界。** 全量的自动波形检查按能量选0.5秒片段、在预期偏移前后0.1秒内求互相关；阈值绝对相关性≥0.9、偏移差≤0.02秒，低于阈值只列待复核，不改原文或填补时间。

下一步：对17条待复核逐一检查缩写/标记分词差异、低相关或静音原因；人工复核典型样本词边界并留证。完成后才能称100条逐词对齐可核验，再生成全片语音、视觉、文本特征及统一时序序列。当前不得把`83条自动通过`写成“100条对齐准确”。

复现命令见`code/problem1/README.md`。初次全量运行因6条缺失TextGrid**有意以非零退出**，但仍写出原始`alignment.json`；隔离束宽重试记录为`retry-beam100.json`，`code/problem1/combine_alignments.py`仅替换初次`error`且源哈希、原文、候选区间一致的6条，生成`alignment-effective.json`。原始和重试结果均保留。
