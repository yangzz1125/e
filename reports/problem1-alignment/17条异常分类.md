# 问题一：17条MFA待复核样本分类

依据：`alignment-effective.json`中的17条`needs_review`。脚本`code/problem1/classify_alignment_reviews.py`用原文与MFA词序的差异、波形验证状态自动分类，详细逐条差异在本地`review-classification.json`（含赛题转写，不上传GitHub）。**分类不是人工回听，所有17条仍保持待复核，不修改题面原文和标签。**

| 优先级与类型 | 数量 | 样本ID | 下一步 |
|---|---:|---|---|
| **P0 内容不等价** | 1 | `-HwX2H8Z4hY__2` | 原文含方括号内的“President Ronald Reagan”说话人/旁白标记，MFA将其合成`[bracketed]`一个项目；须听录音确认该部分是否实际发声。不能将一个区间强行拆成三个词，若为非语音标记，保留原文并在对齐文本/映射中显式注明排除规则。 |
| **P1 波形相似度低** | 2 | `-MeTTeMJBNc__13`（0.879）、`-aqamKhZ1Ec__0`（0.770） | 两条词数与词序均一致，自动波形相关性低于预设0.9；对比剪辑音频与完整WAV，检查重采样、编码差异、片段首尾及说话内容，回听词边界。偏移量计算与编辑列表相差小不等于对齐可信。 |
| **P1 波形过静** | 2 | `-mJ2ud6oKI8__1`、`-mJ2ud6oKI8__2` | 束宽100/400的隔离重试生成TextGrid、词序一致，但自动选出的半秒波形能量不足，不能独立核对时钟。需播放音频、查音量与声学质量；保留低可信状态。 |
| **P2 分词口径差异** | 12 | `-9y-fZ3swSY__0`、`__4`、`__8`；`-UuX1xuaiiE__0`、`__1`；`-dxfTGcXJoc__1`、`__6`；`-hnBHBN8p5A__7`；`-wny0OAz3g8__0`、`__1`、`__2`、`__5` | 差异仅在缩写/撇号（如`don't`）、数字后缀（`10th`）或原文`^`标记；去掉标点并连接相邻词后文本相同。记录原文字符跨度到MFA词元的明确对应，再抽样回听边界；**不能因为字符串等价就自动断言声学边界准确**。 |

处理顺序：先P0确认方括号是否可作为语音词；其次P1检查音轨与边界；最后P2固定文本规范化和字符映射规则后抽样回听。每条复核需留下视频ID/片段、原文、音频回听区间、MFA区间、判断与处理状态。未复核的词不参与论文中的“对齐准确率”统计。

复现分类：

```bash
/hy-tmp/E/.venv/bin/python code/problem1/classify_alignment_reviews.py \
  --input /hy-tmp/E/problem1/alignment/alignment-effective.json \
  --output /hy-tmp/E/problem1/alignment/review-classification.json
```

与`reports/problem1-alignment/逐词对齐记录.md`一致：全量媒体100条已备齐，MFA受限合并后83条自动检查通过、17条仍待复核、0条缺失TextGrid；这**不是**第一问三模态特征交付完成。
