# E题环境检查报告

## 范围

仅检查当前Windows会话与`C:\Python311\python.exe`，未安装、升级或卸载软件，未下载模型，未关闭用户程序。PATH未发现不代表整台电脑完全没有该软件；其他虚拟环境尚未全面枚举。

## 1. 硬件

| 项目 | 实测 |
|---|---|
| 操作系统 | Windows 11，build 26200（CIM确认；Python平台字符串仍显示Windows-10） |
| CPU | Intel Core i5-11400H，6核12线程 |
| 内存 | 约15.7 GiB可见；检查时可用约2.1 GiB |
| 独立显卡 | NVIDIA GeForce RTX 3050 Ti Laptop GPU |
| 显存 | 4096 MiB；检查时已占用2075 MiB |
| 驱动 | 572.42；nvidia-smi报告CUDA兼容上限12.8 |
| C盘 | 总208.1 GiB，剩余53.1 GiB |
| E盘 | 总476.9 GiB，剩余246.4 GiB |

判断：适合数据审计、冻结编码器分批提取、小型GRU/MLP训练。4GB显存不适合默认全量微调大型模型。当前可用内存偏少，不宜直接加载并复制2.7GiB非对齐Pickle；先由用户保存工作并关闭不需要的大程序。

## 2. Python基础环境

Python 3.11.6，64位。NumPy 1.26.4、SciPy 1.11.4、Pandas 2.1.4、Matplotlib 3.8.2、scikit-learn 1.4.0、openpyxl 3.1.2、OpenCV、SoundFile均通过实际导入检查。

另检测到seaborn 0.13.2、python-docx 1.2.0。uv与Git在PATH中，conda不在PATH中。

## 3. 当前主要阻塞：深度学习依赖不兼容

| 组件 | 检测结果 |
|---|---|
| torch | 2.13.0+cpu；CPU矩阵计算成功；torch.version.cuda=None；cuda.is_available()=False |
| torchaudio | 2.3.0+cu118，导入失败，libtorchaudio.pyd或其依赖无法加载 |
| torchvision | 0.18.0+cu118，导入失败：operator torchvision::nms does not exist |
| transformers | 安装元数据版本5.14.1；导入BertModel时受torchvision错误阻塞 |

**显卡驱动能识别GPU，但当前PyTorch是CPU版本，不能使用CUDA。** torchaudio和torchvision需要torch 2.3.0+cu118，与当前torch不匹配。不能把“包已安装”当作“模型环境可用”。

`python -m pip check`还发现全局环境中google-generativeai、kokoro-onnx、TensorBoard和TensorFlow相关依赖冲突。它们不是本题必要组件，不应为本题贸然整体修复或升级全局环境。

nvcc未在PATH中发现。使用官方预编译CUDA版PyTorch通常无需另外安装完整CUDA Toolkit，不能把nvcc缺失误判为GPU不可用的根因。

## 4. 问题一与问题三专项依赖

当前Python未找到：opensmile、librosa、mediapipe、whisperx、montreal-forced-aligner、captum、shap、SciencePlots、adjustText、PyMuPDF。

MFA命令未在PATH发现；OpenFace独立程序未全面定位，状态为未确认，不能声称已安装或完全没有。

建议按路线补最小依赖，不全部安装：

- 问题一声学：openSMILE；librosa只在备用路线需要。
- 问题一视觉：MediaPipe或OpenFace二选一先试跑。
- 强制对齐：MFA或WhisperX二选一评估Windows安装与权重需求。
- 问题三：Captum用于IG；只有3个模态的精确Shapley可直接枚举8个联盟，不一定需要SHAP包。
- 绘图：SciencePlots、adjustText按项目绘图规范补齐。

## 5. 媒体与论文工具

FFmpeg和ffprobe执行版本命令成功，当前子进程返回8.1.2。初次PATH查询显示路径目录名含ffmpeg-6.0；目录名不能代表实际版本，实施时需锁定绝对可执行路径并再次核对，以免Windows进程搜索顺序导致版本混用。

XeLaTeX（TeX Live 2025）版本检查通过；pdftoppm 25.02.0通过。尚未编译实际比赛模板或对视频做完整解码测试。

Typst、drawio、mutool和magick未在PATH发现。已有XeLaTeX和pdftoppm，论文编译与PDF转图基础工具足够；不必为此同时安装另一套工具。

## 6. 推荐下一步（尚未执行）

1. 在`E:\MathModelWorkspaces\E题\.venv`创建独立Python 3.11环境，不继承全局site-packages。
2. 从PyTorch官方安装选择器/版本矩阵选择与驱动和Windows兼容的一组torch、torchvision、torchaudio；只装实际需要的组件，不能继续混搭CPU与不匹配的CUDA版本。
3. 安装基础分析库及兼容的Transformers，再测试GPU矩阵运算、BertModel导入和pip check。
4. 按第一问试验路线补特征提取器，权重下载前确认所选模型、体积与许可。
5. 用户关闭不需要的程序后，再检查可用内存与显存，分批提取特征、缓存float32表示。

准备环境的命令示例，未执行：

```powershell
py -3.11 -m venv "E:\MathModelWorkspaces\E题\.venv"
& "E:\MathModelWorkspaces\E题\.venv\Scripts\python.exe" -m pip install --upgrade pip
# 先按官方兼容矩阵安装CUDA版PyTorch相关组件，再安装本题需要的库。
& "E:\MathModelWorkspaces\E题\.venv\Scripts\python.exe" -m pip install numpy scipy pandas matplotlib scikit-learn openpyxl
& "E:\MathModelWorkspaces\E题\.venv\Scripts\python.exe" -m pip check
```

## 7. 结论

- 数据读取与CPU基线：目前可开展，需注意可用内存。
- GPU训练与BERT编码：当前环境不通过，必须先隔离并修复依赖。
- 第一问完整特征提取：缺少关键工具，尚未就绪。
- 第三问归因：需先解决模型环境，再补必要解释工具。
- 论文工具：XeLaTeX与PDF转图工具可用，模板实编译尚待验证。

下一步建议由用户确认后创建项目专用环境；本轮没有执行任何安装。
